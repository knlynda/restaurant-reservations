<%- model_class = Reservation -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>

<%= form_for ([@reservation.table, @reservation]), :html => { :class => 'form-horizontal' } do |f| %>
    <div class="control-group">
      <%= f.label :start_time, :class => 'control-label' %>
      <div id="datetimepicker_start_time" class="controls">
        <%= f.text_field :start_time, 'data-format'=>'yyyy-MM-dd HH:mm:ss' %>
        <span class="add-on">
          <i data-time-icon="icon-time" data-date-icon="icon-calendar"> </i>
        </span>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :end_time, :class => 'control-label' %>
      <div id="datetimepicker_end_time" class="controls">
        <%= f.text_field :end_time, 'data-format'=>'yyyy-MM-dd HH:mm:ss' %>
        <span class="add-on">
          <i data-time-icon="icon-time" data-date-icon="icon-calendar"> </i>
        </span>
      </div>
    </div>
    <div class="control-group">
      <%= f.label :table_id, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :table_id, :class => 'text_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  table_reservations_path(@reservation.table), :class => 'btn' %>
    </div>
<% end %>

